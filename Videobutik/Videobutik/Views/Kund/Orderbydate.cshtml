@model IEnumerable<Videobutik.Models.Kund>

@{
    ViewBag.Title = "Orderbydate";
}

<h2>Orderbydate</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fornamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.efternamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.registerdatum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fornamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.efternamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.registerdatum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
